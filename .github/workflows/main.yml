name: auto-update
on:
  push:
    branches: [ main ]
jobs:
  Automatically-Update:
    name: Spenny
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v3
      - name: bro idk
        run: |
          echo "am I even in the terminal"
          mkdir madeDirFromWorkflow
